apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grafana.sarafu.network
  namespace: grassroots
  labels:
    "use-http01-solver": "true"
spec:
  commonName: grafana.sarafu.network
  secretName: grafana.sarafu.network.cert
  dnsNames:
    - grafana.sarafu.network
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: grafana
  namespace: grassroots
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`grafana.sarafu.network`)
      services:
        - name: grafana
          port: 80
  tls:
    secretName: grafana.sarafu.network.cert

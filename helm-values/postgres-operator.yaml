configKubernetes:
  spilo_privileged: true
  pod_environment_secret: walg-env
  pod_environment_configmap: zalando-override
configAwsOrGcp:
  wal_s3_bucket: pg-physical
connectionPooler:
  connection_pooler_mode: session

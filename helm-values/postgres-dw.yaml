auth:
  existingSecret: postgres-credentials
  database: postgres
volumePermissions:
  enabled: true
primary:
  initdb:
    scriptsConfigMap: postgres-dw-initdb-scipts
    user: postgres
  nodeAffinityPreset:
    type: soft
    key: kubernetes.io/hostname
    values:
      - pool-7cvqlvxkx-cbyqi
metrics:
  enabled: true
audit:
  logConnections: true
  logHostname: true
tls:
  enabled: true
  autoGenerated: true
persistence:
  size: 15Gi
